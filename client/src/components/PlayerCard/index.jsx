import React from 'react';
import { socket } from 'socket.js';

export const PlayerCard = () => {

    socket.on('player', )


    return (
        <div className="card">
            
        </div>
    )
}